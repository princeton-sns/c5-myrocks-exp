DROP TABLE IF EXISTS HOTKEY;
DROP TABLE IF EXISTS KV;

CREATE TABLE HOTKEY (
  	K INT PRIMARY KEY,
  	V INT
);

CREATE TABLE KV (
  	K1 INT PRIMARY KEY,
  	K2 INT,
  	V INT
);

CREATE UNIQUE INDEX K2 ON KV (K2);
